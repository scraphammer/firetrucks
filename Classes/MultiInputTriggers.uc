//=============================================================================
// MultiInputTriggers. Triggers that take multiple inputs.
//=============================================================================
class MultiInputTriggers extends Triggers abstract;

function DummyCallback(Actor other, Pawn eventInstigator, name eventThatFired) {
  // subclasses to implement
}